import React from 'react';
import { StyledDiv } from './SectionBottom.styles'

export default function SectionBottom() {
    return (
        <StyledDiv>
            <h3>Get Started with React</h3>
            <div>
                React makes it painless to create interactive UI's. Design
                simple views for each state in your application.
            </div>
            <p>
                reacjs.org
            </p>
        </StyledDiv>
    );
}
